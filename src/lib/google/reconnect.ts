export const GOOGLE_ANALYTICS_SETTINGS_PATH = '/admin/analytics?tab=settings'

/** Forces a fresh OAuth consent (deletes stored tokens first). */
export const GOOGLE_RECONNECT_AUTH_PATH = '/api/google/auth?reconnect=1'
