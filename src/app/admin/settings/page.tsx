import { CmsPlaceholder } from "@/components/admin/cms-placeholder";

export default function AdminSettingsPage() {
  return (
    <CmsPlaceholder
      title="Site Settings"
      description="Configure branding, social links, contact details, donation settings, and livestream URLs in one place."
      primaryActionLabel="Save Settings"
    />
  );
}
