import { CmsPlaceholder } from "@/components/admin/cms-placeholder";

export default function AdminSermonsPage() {
  return (
    <CmsPlaceholder
      title="Sermons Manager"
      description="Manage sermon entries, video URLs, audio links, preacher information, and publication status."
      primaryActionLabel="Add Sermon"
    />
  );
}
